# Анализ библиотек для Drag-and-Drop с Grid Layout

## Контекст проекта

**Проект:** Конструктор сайтов (Site Builder)  
**Стек:** React 19, TypeScript, Vite, RTK Query, SCSS  
**Требования:** Drag-and-drop функциональность с размещением блоков по сетке

## Рекомендуемые решения

### 🥇 Вариант 1: react-grid-layout (РЕКОМЕНДУЕТСЯ)

**Пакет:** `react-grid-layout`

#### Преимущества:
- ✅ **Специализированная библиотека** для grid + drag-and-drop
- ✅ **Готовая grid-система** - не нужно реализовывать сетку самостоятельно
- ✅ **Адаптивность** - встроенная поддержка responsive breakpoints
- ✅ **Высокая производительность** - оптимизирована для работы с большим количеством элементов
- ✅ **Зрелая библиотека** - активно поддерживается, много примеров и документации
- ✅ **TypeScript поддержка** - есть типы из коробки
- ✅ **Гибкая настройка** - можно настроить размеры колонок, отступы, минимальные/максимальные размеры элементов
- ✅ **Сохранение состояния** - легко сериализуется в JSON для сохранения в Firebase

#### Недостатки:
- ⚠️ Зависимость от `react-draggable` (может быть избыточной, если не используется отдельно)
- ⚠️ Менее гибкая кастомизация по сравнению с dnd-kit

#### Установка:
```bash
yarn add react-grid-layout
yarn add -D @types/react-grid-layout
```

#### Пример использования:
```tsx
import GridLayout from 'react-grid-layout';
import 'react-grid-layout/css/styles.css';
import 'react-grid-layout/css/resizable.css';

const layout = [
  { i: 'a', x: 0, y: 0, w: 1, h: 2 },
  { i: 'b', x: 1, y: 0, w: 3, h: 2 },
  { i: 'c', x: 4, y: 0, w: 1, h: 2 }
];

<GridLayout
  className="layout"
  layout={layout}
  cols={12}
  rowHeight={30}
  width={1200}
  onLayoutChange={(layout) => {
    // Сохранение в Firebase через RTK Query
  }}
>
  <div key="a">Блок A</div>
  <div key="b">Блок B</div>
  <div key="c">Блок C</div>
</GridLayout>
```

#### Размер пакета:
- `react-grid-layout`: ~50KB (gzipped)

---

### 🥈 Вариант 2: @dnd-kit (АЛЬТЕРНАТИВА)

**Пакеты:** `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities`

#### Преимущества:
- ✅ **Современная архитектура** - построена на современных React паттернах (hooks)
- ✅ **Максимальная гибкость** - полный контроль над поведением drag-and-drop
- ✅ **Модульность** - можно использовать только нужные части
- ✅ **Отличная производительность** - оптимизирована для React 18+
- ✅ **TypeScript first** - написана на TypeScript
- ✅ **Accessibility** - встроенная поддержка доступности
- ✅ **Кастомизация** - полный контроль над визуальными эффектами и анимациями
- ✅ **Меньший размер** - более легковесная по сравнению с react-grid-layout

#### Недостатки:
- ⚠️ **Нет встроенной grid-системы** - нужно реализовывать grid самостоятельно (CSS Grid или Flexbox)
- ⚠️ **Больше кода** - требуется больше boilerplate для реализации grid layout
- ⚠️ **Сложнее для новичков** - более крутая кривая обучения

#### Установка:
```bash
yarn add @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
```

#### Пример использования:
```tsx
import {
  DndContext,
  closestCenter,
  KeyboardSensor,
  PointerSensor,
  useSensor,
  useSensors,
} from '@dnd-kit/core';
import {
  arrayMove,
  SortableContext,
  sortableKeyboardCoordinates,
  useSortable,
} from '@dnd-kit/sortable';
import { CSS } from '@dnd-kit/utilities';

// Нужно реализовать grid самостоятельно через CSS Grid
const GridItem = ({ id, children }) => {
  const {
    attributes,
    listeners,
    setNodeRef,
    transform,
    transition,
  } = useSortable({ id });

  const style = {
    transform: CSS.Transform.toString(transform),
    transition,
  };

  return (
    <div ref={setNodeRef} style={style} {...attributes} {...listeners}>
      {children}
    </div>
  );
};

<DndContext
  sensors={sensors}
  collisionDetection={closestCenter}
  onDragEnd={handleDragEnd}
>
  <SortableContext items={items} grid>
    <div className="grid-container" style={{ display: 'grid', gridTemplateColumns: 'repeat(12, 1fr)' }}>
      {items.map((item) => (
        <GridItem key={item.id} id={item.id}>
          {item.content}
        </GridItem>
      ))}
    </div>
  </SortableContext>
</DndContext>
```

#### Размер пакета:
- `@dnd-kit/core`: ~15KB (gzipped)
- `@dnd-kit/sortable`: ~8KB (gzipped)
- `@dnd-kit/utilities`: ~3KB (gzipped)
- **Итого:** ~26KB (gzipped)

---

## Сравнительная таблица

| Критерий | react-grid-layout | @dnd-kit |
|----------|-------------------|----------|
| **Grid система** | ✅ Встроенная | ❌ Нужно реализовывать |
| **Размер пакета** | ~50KB | ~26KB |
| **Простота использования** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **Гибкость** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Производительность** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **TypeScript** | ✅ (через @types) | ✅ Нативный |
| **Адаптивность** | ✅ Встроенная | ⚠️ Нужно реализовывать |
| **Документация** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Активность** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Поддержка React 19** | ✅ | ✅ |

---

## Рекомендация

### 🎯 Для вашего проекта рекомендуется: **react-grid-layout**

#### Обоснование:

1. **Специализация под задачу**: Конструктор сайтов требует именно grid layout с drag-and-drop, что является основной функциональностью `react-grid-layout`

2. **Быстрая разработка**: Меньше времени на реализацию, больше времени на бизнес-логику

3. **Адаптивность из коробки**: Важно для конструктора сайтов - пользователи создают сайты, которые должны работать на разных устройствах

4. **Интеграция с Firebase**: Layout легко сериализуется в JSON формат для сохранения в Firebase

5. **Соответствие стеку**: Отлично работает с React 19, TypeScript, RTK Query

6. **Простота поддержки**: Команде будет проще поддерживать код на основе готового решения

### 📝 Когда выбрать @dnd-kit:

- Если нужна максимальная кастомизация визуальных эффектов
- Если grid-система должна быть нестандартной (не прямоугольная сетка)
- Если критичен размер бандла
- Если команда готова потратить больше времени на реализацию

---

## План внедрения (для react-grid-layout)

1. **Установка зависимостей**
   ```bash
   yarn add react-grid-layout
   yarn add -D @types/react-grid-layout
   ```

2. **Создание компонента GridBuilder**
   - Компонент для работы с grid layout
   - Интеграция с RTK Query для сохранения layout в Firebase
   - Типизация layout данных

3. **Расширение типов сайта**
   - Добавить поле `layout` в `ISiteDTO` для хранения grid конфигурации

4. **Создание палитры блоков**
   - Компонент с доступными блоками для перетаскивания
   - Интеграция с grid layout

5. **Стилизация**
   - Кастомизация стилей react-grid-layout под дизайн проекта

---

## Полезные ссылки

- [react-grid-layout GitHub](https://github.com/react-grid-layout/react-grid-layout)
- [react-grid-layout Документация](https://github.com/react-grid-layout/react-grid-layout#readme)
- [@dnd-kit Документация](https://docs.dndkit.com/)
- [Примеры react-grid-layout](https://github.com/react-grid-layout/react-grid-layout/tree/master/test/examples)

---

## Заключение

Для конструктора сайтов с требованием drag-and-drop и grid layout, **react-grid-layout** является оптимальным выбором, так как предоставляет готовое решение, которое можно быстро интегрировать и адаптировать под нужды проекта. Это позволит сфокусироваться на бизнес-логике конструктора, а не на реализации базовой функциональности перетаскивания и сетки.


